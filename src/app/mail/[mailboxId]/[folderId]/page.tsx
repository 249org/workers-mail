export const dynamic = "force-dynamic";
export const revalidate = 0;

/**
 * Folder is a URL segment only. The mailbox layout owns the workspace so folder
 * switches do not remount the live socket or wait on D1 before painting.
 */
export default function FolderPage() {
  return null;
}
